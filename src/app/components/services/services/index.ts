// Main component
export { ServicesSection } from './ServicesCardSlider';

// Sub-components (for flexibility)
export { ServiceCard } from './ServiceCard';

// Types (in case needed elsewhere)
export type { ServiceItem, ServiceColorTheme } from '@/types/services/services.type';

// Data (in case needed elsewhere)
export { servicesData } from '@/data/services/services.data';

// Hook (in case needed elsewhere)
export { useSlider } from '@/hooks/sliders/useSlider';